import { Layout as enUS } from './en-us';
import { Layout as esES } from './es-es';
import { Layout as esLA } from './es-la';

export const Layouts = {
  'en-us': enUS,
  'es-es': esES,
  'es-la': esLA,
};

export * from './type';
